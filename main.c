#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	float a;
int	x = 1;
	int y = 3;
	a = 7.5;
	int mat = 123;
	int idade = 25;
	printf ("O valor de a = %.1f", a);
	printf ("\n\nResultado da soma:%d tem %d anos", mat, idade);
	printf("\n\nResultado da soma: %d", x+y);
	
    return 0;
}
